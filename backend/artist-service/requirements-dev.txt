# Dependencias solo de test/CI — no se instalan en la imagen de producción
# (el Dockerfile solo usa requirements.txt).
-r requirements.txt
pytest==8.3.5
pytest-asyncio==0.25.3
httpx==0.28.1
ruff==0.9.6
